<ol>
  <% @comments.each do |comment| %>
    <li><%= link_to comment.description, recipe_comment_path(comment.recipe, comment), class: "js-showComment", :'recipe-id' => comment.recipe.id, :'comment-id' => comment.id %></li>
  <% end %>
</ol>